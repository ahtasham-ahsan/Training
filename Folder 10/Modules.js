// Modules
// Modules in JavaScript are reusable pieces of code that can be exported from one file and imported into another. They allow developers to split their code into smaller, more manageable, and reusable files.

import multiply, { add, subtract } from './math.js';    

console.log(add(2, 3));        // 5
console.log(subtract(7, 2));   // 5
console.log(multiply(3, 4));   // 12

console.log('Hello  ');
